#!/bin/bash

## Description: Install TYPO3 10 and TYPO3 11 with imageopt linked to it.
## Usage: install-all
## Example: "ddev install-all"

ABSOLUTE_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/$(basename "${BASH_SOURCE[0]}/.")"

$ABSOLUTE_PATH/install-v10
$ABSOLUTE_PATH/install-v11
